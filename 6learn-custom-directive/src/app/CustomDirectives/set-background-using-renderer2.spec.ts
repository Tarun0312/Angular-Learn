import { SetBackgroundUsingRenderer2 } from './set-background-using-renderer2';

describe('SetBackgroundUsingRenderer2', () => {
  it('should create an instance', () => {
    const directive = new SetBackgroundUsingRenderer2();
    expect(directive).toBeTruthy();
  });
});
